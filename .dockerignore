# Dependencies
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output
.next
out
build

# Version control
.git
.gitignore

# Environment files - NOT ignored because Next.js needs them during build
# (Better Auth, database, etc. initialize at module load time)
# NOTE: .env is only in the builder stage, NOT in the final production image

# IDE / OS files
.vscode
.idea
.DS_Store
*.pem

# Vercel
.vercel

# TypeScript build info
*.tsbuildinfo
next-env.d.ts

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# Documentation
README.md
*.md

# Drizzle migrations (not needed at runtime)
drizzle
